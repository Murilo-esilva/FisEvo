import{supabase}from'./supabase-config.js';
const message=document.querySelector('#auth-message'),login=document.querySelector('#login-form'),signup=document.querySelector('#signup-form');
function show(text,error=false){message.hidden=false;message.textContent=text;message.className='notice '+(error?'error':'success')}
const{data:{session}}=await supabase.auth.getSession();if(session)location.replace('index.html');
login.addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(e.target);const{error}=await supabase.auth.signInWithPassword({email:fd.get('email'),password:fd.get('password')});if(error)show('Não foi possível entrar. Confira seu e-mail e senha.',true);else location.replace('index.html')});
signup.addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(e.target);if(fd.get('password')!==fd.get('confirmPassword'))return show('As senhas não conferem.',true);const{error}=await supabase.auth.signUp({email:fd.get('email'),password:fd.get('password'),options:{emailRedirectTo:new URL('login.html',location.href).href}});if(error)show(error.message,true);else show('Conta criada. Verifique seu e-mail para confirmar o cadastro.')});
document.querySelector('#toggle-auth').onclick=()=>{const create=signup.hidden;signup.hidden=!create;login.hidden=create;document.querySelector('#toggle-auth').textContent=create?'Já tenho conta':'Criar conta';document.querySelector('#auth-title').textContent=create?'Crie sua conta':'Evolua um dia de cada vez'};
document.querySelector('#reset-password').onclick=async()=>{const email=prompt('Informe o e-mail da conta:');if(!email)return;const{error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:new URL('login.html',location.href).href});show(error?error.message:'E-mail de recuperação enviado.',!!error)};
